// Old analysis chat handler tests removed — the handlers have been replaced by entity graph chat.
// TODO: physically delete this file.
import { describe, it } from "vitest";
describe("analysis-chat-handlers (removed)", () => {
  it("placeholder", () => {});
});
