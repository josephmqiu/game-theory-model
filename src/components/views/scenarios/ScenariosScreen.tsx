import type { ReactNode } from 'react'

export function ScenariosScreen(): ReactNode {
  return (
    <div className="flex-1 p-6">
      <h1 className="text-lg font-bold text-text-primary mb-4">Scenarios</h1>
      <p className="text-sm text-text-muted">Central thesis, scenario cards, tail risks, and final test — populated after Phase 9</p>
    </div>
  )
}
