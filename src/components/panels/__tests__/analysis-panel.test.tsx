// Old analysis panel tests removed — the panel has been replaced by entity graph canvas.
// TODO: physically delete this file.
import { describe, it } from "vitest";
describe("analysis-panel (removed)", () => {
  it("placeholder", () => {});
});
